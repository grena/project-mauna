/*
|--------------------------------------------------------------------------
| Models de base
|--------------------------------------------------------------------------
*/
define([
    'angular',
    'models/index'
], function(angular, model) {

    'use strict';

    angular.module(model.name).controller('Base', [
        function () {
        }
    ]);
});
